<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Default: block cleartext traffic to enforce HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificates -->
            <certificates src="system" />
            <!-- Trust user-added certificates (for corporate/self-signed certs) -->
            <certificates src="user" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext only for localhost (required by the local HTTP proxy
         that bridges network streams to the mpv player) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
